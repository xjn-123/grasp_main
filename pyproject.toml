[project]
name = "grasp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "X", email = "2592401352@qq.com" }
]
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "opencv-python>=5.0.0.93",
    "pyapriltags>=3.4.3.1",
]

[project.scripts]
grasp = "grasp:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
