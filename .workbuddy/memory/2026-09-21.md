# 2026-09-21

## Pylance 不自动补全排查

- 环境：uv 管理，`uv sync` 后项目以 editable 方式装进 `.venv`（`grasp.pth` → `src`），Python 3.11.14，依赖 numpy / opencv-python / pyapriltags 齐全。
- 定位到的问题：`.vscode/settings.json` 里既没有指定解释器，又把两个 `python.analysis.*` 键误嵌套进了 `python.analysis.aiCodeActions` 对象内部。
- 已修复 `.vscode/settings.json`：
  - 补 `python.defaultInterpreterPath` = `${workspaceFolder}/.venv/Scripts/python.exe`
  - 补 `python.analysis.extraPaths` = `["src"]`
  - 把误嵌套的 `completeFunctionParens` / `autoImportCompletions` 提到正确层级
- 注意点：`.venv` 和 `.vscode` 都在 `robot_grasp_test` 目录，父目录 `arm_disorderly_swap` 没有 venv；VS Code 需要从 `robot_grasp_test` 这一层打开文件夹，否则 Pylance 拿不到解释器。
- 后续确认：Pylance 2026.3.1 已正确安装并激活，输出面板里**没有叫 "Pylance" 的通道**——新版日志通道名为 `Python Language Server`（中文界面显示为「Python 语言服务器」），排查日志要看这个通道。Pylance 是懒激活（onLanguage:python / workspaceContains:pyproject.toml）。

## 分析 Pylance 日志（robot_grasp_test 窗口）

日志确认：解释器已正确切到 `.venv\Scripts\python.exe`（environmentName 由 "grasp (3.11.14)" 变为 "defaultInterpreterPath: 3.11.14.final.0"），索引 53 文件完成，7 个源文件全部识别 —— 该窗口 Pylance 已恢复正常。
发现三个遗留问题：
1. 存在 `<default>` 服务用全局 Python 3.8.0，工作区外文件会被它接管。
2. `[Editable Installs] No sandbox available ... requires Python 3.13 or later` → `grasp.pth` 无法动态解析，靠 extraPaths 兜住。
3. 另一个窗口 `robot_grasp_show` 也用 Python 3.8.0 且无 venv。
已把长期约定写入 MEMORY.md。
